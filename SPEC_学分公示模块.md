# 学分公示模块 — 需求规格 v1.2（已确认）

> 状态：**✅ 已确认，可开工**。目标版本 v2.16.0；v2.16.1 落实 Q6/Q7（最高/最低分、续航榜）
> 编写：2026-09-06　基于老板原话「学分公示模块，全面学分变动分析（本周/今日/本月），Top10 排名榜、进步最大，图表+数据多种直观形式，可导出本月学分排名图片」
> 决策记录：见第九节（老板 2026-09-06 逐项拍板；Q6/Q7 为 2026-09-06 追加拍板）

---

## 一、模块定位（先说清楚它跟「数据分析」不是一回事）

仓库里已经有一个 **数据分析 → 学分分析** 页，里面是平均学分、中位数、标准差、区间分布、进步/退步最大、近 7 天趋势。

**新模块不替换它，两者分工不同：**

| | 数据分析（现有） | 学分公示（新建） |
|---|---|---|
| 给谁看 | 班主任自己 | 全班 / 家长群 / 教室张贴 |
| 关心什么 | 分布是否健康、离散程度 | 谁排前面、谁在进步、这周发生了什么 |
| 输出 | 屏幕上看 | **要能导出成图片发出去** |
| 口径 | 静态截面（当前学分） | **周期对比**（今日/本周/本月/学期） |

所以「学分公示」= **排行榜 + 周期变动 + 可导出张贴**，是一个对外输出物，不是又一个统计图页面。

---

## 二、页面入口与权限

- 侧栏 + 更多抽屉新增一页：**🏆 学分公示**（`page-publicity`）
- **班委模式：可见**（加入 `COMMITTEE_PAGES` 白名单第 7 页）。
  理由：公示内容本身就是公开的，班委需要协助出图；且页面不含家长信息，导出图片也不含。
- 只读页面：不含任何增删改操作，点学生只弹详情。

---

## 三、时间周期（口径必须钉死，否则数字对不上）

| 周期 | 口径 | 说明 |
|---|---|---|
| 今日 | 自然日 00:00 ~ 现在 | 按流水 `time` 时间戳 |
| 本周 | **周一 00:00** ~ 现在 | 中国习惯周一为起点（不是周日） |
| 本月 | 自然月 1 号 00:00 ~ 现在 | |
| 本学期 | 设置页新增「开学日期」→ 今天 | **需要新配置**，默认取当年 9 月 1 日（若当前月份 < 9 则取当年 3 月 1 日），可在设置页改 |
| 全部 | 不限制 | |

**统一规则**：所有周期统计一律从 `state.operations` 流水实时聚合，**不读 `student.credit` 快照**。
（v2.15.0 之后流水是唯一真相来源，快照已能保证与流水一致，但周期增量只能靠流水算）

---

## 四、概览数据卡（周期联动 + 两张不联动卡）

顶部小卡，切周期时一起变（第 1~6 张按周期聚合）：

1. **变动人次**：周期内有流水的学生数 / 全班人数（参与率）
2. **加分**：人次 + 总分
3. **扣分**：人次 + 总分
4. **净变化**：加分总分 + 扣分总分（带正负色）
5. **人均变动**：净变化 ÷ 全班人数
6. **最活跃学生**：周期内流水条数最多的人
7. **🏆 最高分**：当前全班最高学分（**不分周期**，并列取学号小者；显示分数 + 姓名）
8. **⚠️ 最低分**：当前全班最低学分（**不分周期**；<80 分时数字标红警示）

---

## 五、榜单（核心，这是老板要的重点）

| 榜单 | 口径 | 默认 |
|---|---|---|
| 🥇 **学分榜 Top10** | 按**当前总学分**降序，并列按学号升序 | 展示 |
| 📈 **进步榜 Top10** | 按**周期内净增分**降序；净增 ≤0 不进榜 | 展示 |
| 📉 **退步榜 Top5** | 按周期内净减分升序 | **默认折叠**，可开关 |
| ⚠️ **预警榜** | 当前学分 < 80（制度不合格线） | **仅班主任可见**，班委模式隐藏，默认不导出 |
| 🌟 **零扣分榜**（v2.16.1 起改口径） | **「未扣分续航」**：距本人**最后一次扣分**的天数（全程流水，不分周期）；**从未扣分者居首**，其余按天数降序，天数相同按学分 | 展示 |
| 🏅 **单项之星** | 加分次数最多 / 单笔加分最高 | 展示 |

**并列处理**：同分并列显示同一名次（1、1、3），不强行错开 —— 公示场景错开会挨骂。

**进步榜的两种算法，需要老板选（见第九节 Q3）：**
- A. **净增分**（周期内加分 - 扣分）——推荐，直观
- B. 净增分 ÷ 操作次数（"效率"）——会埋没踏实型学生，不推荐

---

## 六、图表（全部 canvas 手绘，零依赖，离线可用）

沿用项目现有风格（`drawDistChart` / `drawRangeChart` / `drawPieChart` 同款纯手绘），**不引入 Chart.js / ECharts CDN** —— 理由是班级现场网络不可控，离线必须能用。

| 图表 | 内容 |
|---|---|
| 学分走势折线 | 近 7 / 30 天**全班人均学分**走势，周期可切 |
| 每日加减分柱状 | 每日加分（绿）与扣分（红）双色堆叠柱，一眼看出哪天出事 |
| 学分分布直方图 | 当前全班学分分布，**画出 100 / 90 / 80 三条制度分界线** |
| 原因分布环形图 | 周期内 Top6 加减分原因占比（读 `state.reasonScores` 与流水） |

配色：加分用 `--success` 绿，扣分用 `--danger` 红（这是学分增减，不是股价，不套红涨绿跌）。

---

## 七、导出图片（老板明确要求）

**形式：canvas 自绘海报，不用 html2canvas**（离线可用、版式可控、不截到半截滚动条）。

- 尺寸：**竖版 1080×1920**（手机全屏，发家长群/朋友圈正好），另提供 **1080×1350 紧凑版**
- 内容：
  1. 顶部：班级名 + 班级格言 + 头像（读 `className` / `classMotto` / `classAvatar`）
  2. 副标题：**「2026 年 9 月学分公示」**（跟着周期变）
  3. 主体：本月学分榜 Top10（名次 / 姓名 / 学分 / 等级徽章）
  4. 底部：进步榜 Top5 摘要 + 生成时间 + 「由班主任工作台生成」小字
- 交互：点「导出图片」→ 生成 → 预览弹窗 → **长按/右键保存**（移动端可直接存相册）
- 文件名：`{班级名}_学分公示_{YYYY-MM}.png`
- **默认不导出**：退步榜、预警榜（避免当众处刑，可在设置里打开）

---

## 八、隐私开关（建议做，成本很低）

设置页新增一项：**公示姓名显示方式**
- 全名（默认，班级内部用）
- 姓 + `*`（如 `张*`）—— 发家长群/公开场合用
- 仅学号

导出图片时沿用这个开关。

---

## 九、决策记录（老板 2026-09-06 拍板）

| # | 议题 | 结论 |
|---|---|---|
| Q1 | 模块形态 | ✅ **独立新页**「🏆 学分公示」，现有数据分析页不动 |
| Q2 | 班委能否看 | ✅ 能看，加入 `COMMITTEE_PAGES` 白名单第 7 页 |
| Q3 | 进步榜算法 | ✅ **周期内净增分**（加分总和 − 扣分总和） |
| Q4 | 退步榜 / 预警榜 | ✅ **保守处理**：退步榜默认折叠可展开；预警榜仅班主任可见、班委隐藏；两者默认不进导出图（设置可开） |
| Q5 | 导出图片尺寸 | ✅ **竖版 1080×1920**，另提供 **1080×1350 紧凑版**，预览弹窗里可选 |
| Q6 | 概览卡补全（2026-09-06 追加） | ✅ 除周期统计外，**再展示当前最高分与最低分**（含姓名；最低 <80 标红），分数口径=当前快照，不随周期变 |
| Q7 | 零扣分榜口径（2026-09-06 追加） | ✅ 老板要求「记录最长时间没扣分的同学」→ **未扣分续航榜**：按距上次扣分天数排（全程流水），从未扣分者居首，每人标签显示「从未扣分」/「N 天」 |
| Q8 | 班委操作端（2026-09-06 追加） | ✅ 确认并入：公示页在班委白名单第 7 页，侧栏 + 更多抽屉双入口，只读可看；预警榜/退步榜隐藏或折叠；**班委不开放导出图**（海报导出按钮对班委拦截） |

---

## 十、顺带发现的两个问题（不在原需求内，建议一起修）

1. **数据分析页的及格/优秀线口径过时**
   现在写的是「及格率 (≥10)」「优秀率 (≥25)」，但 v2.14.0 起制度基准已是 **100 分**（`creditLevel`：≥100 优秀 / ≥90 合格 / ≥80 一般 / <80 不合格）。
   老口径下几乎全班都"及格+优秀"，数字毫无意义。**建议一并改为 ≥90 合格率 / ≥100 优秀率。**

2. **分布图的分段也是老口径**
   `drawRangeChart` / `drawPieChart` 的分段（如 0-10、10-25）同样基于老基准，需要按 100 分制重划（如 <80 / 80-90 / 90-100 / ≥100）。

---

## 十一、技术约束（跟项目既有约定一致）

- 只改 `index.html`，`sw.js` 只动 `CACHE_NAME`
- 零依赖、零构建，图表纯 canvas 手绘
- 统计逻辑抽成**纯函数**便于回归测试：
  `computeCreditStats(operations, students, range)` / `buildLeaderboard(...)` / `buildTrendSeries(...)` / `drawPublicityPoster(...)`
- 新增 `_v2160_test.js` 回归；v2.16.1 全量 11 套共 236 项（另 `_crypto` 跨设备解密校验）
- 改动后版本号三处同步（登录页 / 侧栏 / SW）
