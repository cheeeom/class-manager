# CONTEXT.md — 项目当前快照

> **接续开发必读入口。** 本文件是「压缩版上下文」，让任何 AI 工具（WorkBuddy / Claude Code / Cursor / Windsurf / Copilot）
> 在拿到仓库的 30 秒内建立全貌，不用从 38KB 的 AGENTS.md 里翻。
> 细节查 `AGENTS.md`（架构与约定），逐版本历史查 `PROGRESS.md`（含每次会话记录与踩坑）。
>
> 最后更新：2026-09-07（v2.17.5 已推送）

---

## 一、项目是什么

**班主任工作台** —— 给幼儿园/中小学班主任用的单页应用，纯前端、零框架、零构建。

- **唯一功能改动文件：`index.html`**（约 45 万字符，内含全部 HTML/CSS/JS）
- 唯一例外：`sw.js` 只改 `CACHE_NAME = 'class-manager-v2.x.x'`
- 线上：GitHub Pages，仓库 `cheeeom/class-manager`（**public**）
- 无 npm、无打包、无框架。改完直接推，刷新即生效

## 二、版本与仓库状态

- 当前 `v2.17.5`（学分操作成功后自动清空备选名单，已推送）
- 上一版 `v2.16.1`（公示概览最高/最低分 + 零扣分续航榜 + 班委端确认）
- ⚠️ 历史摘要里出现过的 `730f8067` **不是有效 commit**（amend 前的临时 sha），别再引用；老快照里写的 `4d72d6c` 是 v2.14.0 的 commit，仅作存档
- 版本号必须**三处同步**，漏一处就会出现"页面显示新版本但缓存是旧的"：
  1. `index.html` 登录页版本号
  2. `index.html` 侧栏版本号
  3. `sw.js` 的 `CACHE_NAME`
- 验证方式：**逐位置 grep**，不要只看 `grep -c`

## 三、推送方式（重要）

云端 `data.json` 是 **AES-GCM-256 密文**（v2.8.0 起），离线不可解。
**禁止整仓 force push / 整目录覆盖**，否则会连带把云端真实数据冲掉。

```bash
export GH_TOKEN=$(cat /d/a/chee777/scripts/_gh_token.txt | tr -d '\r\n')
cd /d/a/chee777/class-manager
node D:/a/chee777/scripts/cm-push-incremental.js "提交信息" index.html sw.js PROGRESS.md
```

- 脚本做**增量推送**，保住云端 `data.json`
- ⚠️ Windows 下 `node` 参数必须用 `D:/...` 正斜杠形式，用 `D:\...` 会被转义吃掉

## 四、测试

十四套回归，**共 315 项**（改动后必须全跑，全绿才能推）：

| 文件 | 项数 | 覆盖 |
|---|---|---|
| `_v290_test.js` | 25 | v2.9.0 班委/合并 |
| `_v2100_test.js` | 19 | 值日轮次制/罚扫 |
| `_v2110_test.js` | 24 | 名单同步/同名配对 + 学生排序（v2.17.4 三态循环/数值比较） |
| `_v2113_test.js` | 13 | wipeAt 重置全链路 |
| `_v2120_test.js` | 11 | 学分原因目录 |
| `_v2130_test.js` | 11 | 班委模式白名单 |
| `_v2140_test.js` | 17 | 批量学分/劳动整改（v2.17.5 操作后自动清空备选名单断言） |
| `_v2150_test.js` | 38 | 学分一致性/图表 100 分制/时间线 |
| `_v2160_test.js` | 42 | 学分公示（周期/榜单/最高最低/续航榜/隐私/海报导出/班委端） |
| `_v2170_test.js` | 32 | 原因目录可编辑（纯函数/迁移/接线）+ 选择器逐层渲染 + 班委署名/扣分开关 |
| `_v2171_test.js` | 17 | 学分体检（基准/流水/快照三账核对纯函数 + 基准改 100 受控修正 + UI 接线） |
| `_v2172_test.js` | 26 | 学生快速选择器（姓名/学号过滤纯函数 + 列表渲染 stub-DOM + 三入口接线 + 旧下拉下线 + 主题样式 + v2.17.3 this 陷阱回归） |
| `_sync_test.js` | 26 | 同步与合并 |
| `_xss_test.js` | 14 | XSS 防护 |

```bash
cd /d/a/chee777/class-manager
for f in _v290 _v2100 _v2110 _v2113 _v2120 _v2130 _v2140 _v2150 _v2160 _v2170 _v2171 _v2172 _sync _xss; do node ${f}_test.js; done
```

**做法**：用正则从 `index.html` 抽取真实函数源码，`eval` 后跑断言 —— 测的是线上代码，不是复制品。

**几个反复踩的坑**：
1. `eval` 解析对象字面量必须加括号：`eval('(' + src + ')')`
2. `eq` 断言**不能传数组**（引用比较必失败），要传基本类型
3. 版本号验证要逐位置 grep，不能只数出现次数
4. 抽取函数用「到行首 `}` 收尾」的规则，**单行定义**（`function x(){...}` 无换行）抓不到 → 换别的断言方式或 stub
5. **canvas 绘制冒烟**（真实执行不抛异常）：stub 万能 ctx（Proxy：任意方法 no-op、`measureText`→{width}、渐变→{addColorStop}）+ stub `document.getElementById`/`window.devicePixelRatio`/`requestAnimationFrame`，把绘制函数真跑一遍。样例见 `_v2160_test.js`「绘制冒烟」节（记得 stub 被引用的 `escapeHtml`/`roundRect`/`rr` 等全局 helper）

## 五、关键架构记忆

### 1. 学分双写模型（v2.15.0 重构）
- `student.credit` 是**快照**，`state.operations[]` 是**流水**
- 真理来源是流水：`credit = student.creditBase + Σ(该学生所有 operations.amount)`
- `creditBase` 为迁移期反推的基线（老学生初始分不全是 100，不能直接按 100 重算）
- **所有学分写入必须走统一入口**，禁止裸写 `student.credit += x`

### 2. smartMerge 并集合并
- 推送前、拉取后都做并集合并
- `students` 按 id 合并、取 `updatedAt` 新的；**学生对象原本没有 updatedAt → 永远取本地**（这是 v2.14.0 及之前学分不一致的根因，v2.15.0 已修）
- `operations` 按 id **去重并集**，永不裁剪（唯一清空途径是 `clearData`）
- 本地空 + 云端有人 → 拦截并从云端恢复（防误清）

### 3. wipeAt 重置戳
- 应用内清空/直接删云端文件都**无效**：空本地保护 + 并集合并会让数据复活
- 唯一正解：`state.wipeAt = Date.now()`，`checkPushSafety` / `applyCloudData` 双端拦截
- `wipeInProgress` 锁屏蔽 wipe 期间的常规推拉，防竞态

### 4. 班委模式
- `sessionStorage.cm_role` + `window.__cmRole`；v2.17.0 起班委入口先「选身份」（`cm_uid`，从已任命岗位选，可匿名兜底），`resolveCmIdentity()` 解析为 `window.__cmIdentity`
- **白名单制**：首页/学分/公示/值日/座次/待办/荣誉 7 页（v2.16.0 起含公示）
- 三重拦截：`navigateTo` 入口 + 导航裁剪 + `exportData/importData/openAddStudentModal` 函数级
- 推送**保留**（禁推会让班委加的学分本地蒸发）；海报导出对班委也拦截（公示页只读）
- **v2.17.0 学分操作开放 + 留痕**：班委在学分页可加减分（扣分受设置开关「允许班委扣分」控制，`cmMinusBlocked()` 拦截三个负值入口 + 减分按钮置灰）；`applyCreditDelta` 对班委操作写 `op.by='cm' / byName / byPost`，时间线紫色小标署名（教师操作不写 by）
- 班委协作设置存本地 `cm_teacher_settings`（不上云，与公示设置同模式）

### 5. 原因目录（v2.17.0 可编辑化，重要）
- **模板与实例分离**：`REASON_CATALOG`/`defaultReasonScores` 降级为「初始模板」，真实目录在 `state.reasonCatalog`（同结构 方向→大类→[原因]），云端 `reasonCatalog` 字段随 data.json 同步
- 设置 → 学分原因目录：方向/大类/原因/默认分值全可增删改（分值仍统一存 `state.reasonScores`），一键恢复预设；老数据迁移：无目录 → 模板，历史 reasons 自定义项并入「其他→自定义原因」组不丢；**已有目录不做模板并集**（删除的预设不复活）
- `reasons` 一维列表降为**派生缓存**（`syncReasonsFromCatalog()` = 目录扁平），任何目录改动必须同步 reasons 与选择器
- 合并：`smartMergeData` 对 reasonCatalog 做**结构并集**（两侧改动都保留，删除项复活属已知取舍）
- 键名限纯中文/数字（escapeAttr 白名单无 emoji），emoji 只进显示文本
- 多级选择器（学分页/批量弹窗）逐层下钻：方向 → 大类 → 原因 + 面包屑返回，数据源读 `state.reasonCatalog`

### 6. 「皮肤」模式（重要模式，沿用）
- v2.12.0 原因选择器、v2.14.0 批量多选：原 `<select>` **保留为隐藏数据载体**（`display:none`），新 UI 只是可视皮肤
- 好处：`quickCredit` / `confirmBatchCredit` / `undo` 等既有逻辑**零改动**

### 7. escapeAttr 白名单
- 白名单 `[a-zA-Z0-9_-一-龥 ]`，**不含 emoji**
- `➕ 加分` 会被剥成 ` 加分` → **目录键必须纯中文**，emoji 只进显示文本

## 六、版本演进（近两日）

| 版本 | 内容 |
|---|---|
| v2.11.0-2 | 学生列表排序（默认学号↑/点学分切）+ 名单同步（按姓名合并保留学分标签，修 115 人重复导入事故） |
| v2.11.3 | 彻底重置：wipeAt 全链路，连云端一起清，其他设备自动对齐 |
| v2.12.0 | 学分原因多级选择器（加分/扣分/其他 → 大类 → 原因，26 项制度原因，点选带分值） |
| v2.13.0 | 班委免密入口，白名单 6 页，档案/成绩/设置/导入导出全屏蔽 |
| v2.14.0 | 学分批量多选；罚扫→劳动整改（教室/公共区/搬水，默认 7/7/1 天）；初始学分统一 100；关闭键美化 |
| v2.15.0 | 学分一致性修复：creditBase 基线 + applyCreditDelta 统一入口 + reconcileCreditDrift 自愈 + smartMerge 按 creditVer 取新 + refreshCreditViews 全视图刷新 |
| v2.15.1 | 热修：时间线不显示（统一渲染/时间兜底/转义）、首页最值错位（Number 规范化）、图表 100 分制口径（自适应 8 档 + 制度四档 + 合格率 ≥90/优秀率 ≥100） |
| v2.16.0 | 学分公示模块：独立公示页 + 周期统计（今日/本周/本月/学期）+ 6 榜单 + 4 图表（canvas 手绘）+ 海报导出（1080×1920 / 1080×1350）+ 隐私开关（全名/张*/仅学号）+ 班委白名单第 7 页 |
| v2.16.1 | 公示增强：概览卡加当前最高分/最低分（不分周期，<80 标红）；零扣分榜改「未扣分续航」口径（距上次扣分天数，从未扣分居首，标签「从未扣分/N 天」）；班委操作端确认（双入口可见、预警/退步受控、导出拦截）；测试 226→236 |
| v2.17.0 | 原因目录可编辑化（模板/实例分离，设置页方向/大类/原因/分值全增删改+恢复预设，随云同步）；多级选择器逐层下钻（Cascader 式面包屑）；班委操作学分+署名留痕（入口选身份、时间线紫标、扣分开关）；测试 236→268 |
| v2.17.1 | 学分体检（设置页「数据管理」新入口）：逐生核对 当前分 = 入班基准 creditBase + 全部流水，漂移/基准≠100 可视化，一键校准快照、双确认把基准纠回 100；背景=公示最低分与扣分流水矛盾的诊断（两生无加分、扣得多的反而分低 → 老数据反推基准≠100）；最高/最低分姓名放大 15px 热修并入本版；测试 268→285 |
| v2.17.2 | 学生快速选择器：任命班委/座位/值日共享弹窗从「原生下拉滚动找」改为「搜索框（姓名/学号即时筛选）+ 点行即生效 + 回车唯一匹配直选 + 现任标注」，旧的 `<select>` 确认按钮流程下线；暗色主题适配（跟随弹窗变量），顺手修 v2.17.1 学分体检弹窗暗色白字隐形（强制白底）；测试 285→308 |
| v2.17.3 | 热修：选择器输入不筛选（始终全量）——根因是内联 `oninput="onStudentSearchInput()"` 裸调用导致 `this` 指向 window、`this.value` 恒 undefined → 改为函数内按 id 取输入框取值；用 headless Chromium 真实事件路径复现并验证修复（王→2、罗→1、无匹配→空态）；新增 3 条防回归；测试 308→311 |
| v2.17.4 | 修复「学生模块点学分排序没排对」：旧版只有 序号⇄学分↓ 两态（永远排不出升序、再点直接跳回序号，观感=没排序）；改为三态循环（↓→↑→序号）+ 学分比较强制 Number()（防导入字符串残留如 '80' 排错位）+ 列头 title 更新；真机点击验证三态闭环；_v2110 排序用例 21→24；测试 311→314 |
| v2.17.5 | 学分批量操作体验：applyCreditBulk 成功后**自动清空备选名单**（清 `_creditSelectedIds` + 重渲染 chips + 复位学生搜索框与下拉），免手动点「清空」；_v2140 17 项断言；测试 314→315 |

## 七、待办

- [ ] 老板验收 v2.17.0：设置页改原因目录（加方向/组/原因、改分值、恢复预设）→ 学分页选原因看逐层级联 → 班委入口选身份 → 加/扣分后回教师端时间线看署名
- [ ] 老板验收 v2.16.1：公示页概览最高/最低分卡 → 零扣分榜看「N 天/从未扣分」标签 → 班委入口进公示页只读、导出被拦
- [ ] 老板验收 v2.14.0（批量学分 / 劳动整改 / 关闭键）
- [ ] GitHub 工单（清 force push 后旧 sha 缓存视图）回复后，验证旧 sha 返回 404

## 八、备份

- `D:\a\chee777\backups\data.json.bak-20260905-154027` —— 清空前密文备份，可回滚
- 仓库外（不进 public 仓库）：`D:\a\chee777\名单同步_26幼2_59人.txt`（59 人含两名同名）
